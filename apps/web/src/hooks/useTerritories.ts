"use client";

import { useTerritories } from "./useLocations";

/**
 * Territories available to the current user for filters/forms.
 * @deprecated Import from `./useLocations` instead.
 */
export { useTerritories };
